# This folder contains our own patches for some packages

## Patch Log
1. Expo-location@8.3.0 package seems to have a race condition when using getHeadingAsync(), fix by making sure subscription is not undefined before calling its remove() func.
